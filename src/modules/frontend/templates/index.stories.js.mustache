import React from 'react';
import {{frontend.itemName}} from './index.js';

// This default export determines where your story goes in the story list

export default {
  title: 'Example',
  component: {{frontend.itemName}},
};

const Template = (args) => <{{frontend.itemName}} {...args} />;

export const FirstStory = Template.bind({});