const buildFullStackApp = require('./utils/actions/fullStackAppGenerator');
const generateReactComponent = require('./utils/actions/generateReactComponent');

module.exports = {
  buildFullStackApp,
  generateReactComponent,
};
